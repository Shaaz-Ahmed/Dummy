//Custom Pagination Styles

// @mui material components
import { styled } from "@mui/material/styles";

export default styled("div")(() => ({
  display: "flex",
  justifyContent: "center",
  marginTop: "20px", // spacing around the pagination
}));
